# Gemini Notes

This file contains notes for the Gemini CLI agent.

## Project Structure

- `CMakeLists.txt`: Main CMake file.
- `build/`: Build directory.
- `include/`: Header files.
- `resources/`: Game assets.
- `src/`: Source files.
- `thirdparty/`: Third-party libraries.

## How to Build

The user has specified not to run CMake.

## Notes

- The user wants me to fix C++ code.
- I should not run the code unless asked.
- I should check if everything is good, which implies looking for compilation errors and logical errors.
